using Microsoft.AspNetCore.Identity;
using SpaCenter.Core.DTO;
using SpaCenter.Core.Entities;

namespace SpaCenter.Services.Spas
{
    //public interface IAccountRepository
    //{
    //    Task<IdentityResult> SignUpAsync(SignUpItem model);

    //    Task<string> SignInAsync(SignInItem model);
    //    Task<bool> IsUserNameExisted(string username);
    //    Task<bool> RemoveUser(int id, CancellationToken cancellationToken = default);
    //}
}